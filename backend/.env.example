PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=chainpay
DB_PASSWORD=           # required — no default; server fails fast if missing
DB_NAME=chainpay_db
DB_SSLMODE=disable
EXECUTOR_PRIVATE_KEY=  # required for on-chain calls
CHAIN_PAY_CONTRACT=    # required for on-chain calls
LIFI_API_KEY=          # required for Li.Fi calldata generation
